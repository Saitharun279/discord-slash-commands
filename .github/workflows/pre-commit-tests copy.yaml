name: pre-merge-checks
on:
  push:
    branches: 
      test/issue#257
jobs:
  Run-Tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm test
